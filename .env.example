# Used by docker-compose.yml when building the frontend image.
# Point this at wherever the backend container/service is reachable from
# the browser (not from inside Docker) — e.g. your VM's public IP/domain
# and the port you expose the backend on.
VITE_API_URL=http://YOUR_SERVER_IP:4000
VITE_API_KEY=
